logger:
  level: "info" # info or error

http:
  listen: ":8081"

storage:
  type: "memory" # "memory" or "redis" or "postgres"
  redis:
    addr: "localhost:6379"
    password: ""
    db: 0
  postgres:
    dsn: "test_DSN_replaced_by_SECRETS" #migration = "migrations"

migrationsPath: "./migrations"