logger:
  level: "info" # info or error

http:
  listen: ":8081"

storage:
  type: "memory" # "memory" or "redis" or "postgres"
  redis:
    addr: "localhost:6379"
    password: ""
    db: 0
  postgres:
    dsn: "" #migration = "migrations"

migrationsPath: "./migrations"